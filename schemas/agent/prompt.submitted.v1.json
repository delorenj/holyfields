{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://33god.dev/schemas/agent/prompt.submitted.v1.json",
  "title": "Agent Prompt Submitted Event",
  "description": "Emitted when a user submits a prompt to an agent. Producer is the agent runtime (e.g. Claude Code via bloodbank/services/agent-hooks/claude/publish.py on UserPromptSubmit). Carries the raw prompt text alongside repo state at submission time. Consumers use it to attribute downstream tool invocations to a user intent and to build retrospective views of what was asked.",
  "type": "object",
  "allOf": [
    { "$ref": "../_common/cloudevent_base.v1.json" }
  ],
  "properties": {
    "type": {
      "const": "agent.prompt.submitted",
      "description": "Locked event type for this schema."
    },
    "domain": {
      "const": "agent",
      "description": "Locked domain for this schema."
    },
    "data": {
      "type": "object",
      "description": "Prompt-submission payload.",
      "properties": {
        "session_id": {
          "$ref": "../_common/types.v1.json#/$defs/uuid",
          "description": "Identifier of the session the prompt belongs to. Matches the session_id from the corresponding agent.session.started event."
        },
        "prompt_text": {
          "type": "string",
          "description": "Raw prompt text as submitted by the user. May be truncated by the producer at its discretion; see prompt_length for the original size before any truncation."
        },
        "prompt_length": {
          "type": "integer",
          "minimum": 0,
          "description": "Character length of the original prompt before any truncation. Lets consumers detect truncation by comparing against `len(prompt_text)`."
        },
        "working_directory": {
          "type": "string",
          "description": "Absolute path the agent is operating in at prompt-submit time."
        },
        "git_branch": {
          "type": "string",
          "description": "Git branch at prompt-submit time. Empty string when not in a git repo."
        }
      },
      "required": ["session_id", "prompt_text", "prompt_length"],
      "additionalProperties": false
    }
  },
  "required": ["data"]
}
