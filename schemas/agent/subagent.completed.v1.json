{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://33god.dev/schemas/agent/subagent.completed.v1.json",
  "title": "Agent Subagent Completed Event",
  "description": "Emitted when a subagent (a Task-tool spawned helper) finishes. Producer is the agent runtime (e.g. Claude Code via .claude/hooks/bloodbank-publisher.sh on SubagentStop). Distinct from agent.session.ended: the parent session continues; only the subagent has terminated. The session_id field points to the PARENT session so subagent activity rolls up under the originating session.",
  "type": "object",
  "allOf": [
    { "$ref": "../_common/cloudevent_base.v1.json" }
  ],
  "properties": {
    "type": {
      "const": "agent.subagent.completed",
      "description": "Locked event type for this schema."
    },
    "domain": {
      "const": "agent",
      "description": "Locked domain for this schema."
    },
    "data": {
      "type": "object",
      "description": "Subagent-completion payload.",
      "properties": {
        "session_id": {
          "$ref": "../_common/types.v1.json#/$defs/uuid",
          "description": "Identifier of the PARENT session that spawned the subagent. Subagent activity rolls up under this session."
        },
        "agent_type": {
          "type": "string",
          "description": "Type/name of the subagent that finished (e.g. 'general-purpose', 'demo-architect'). Producer-defined; treat unknown values as opaque."
        },
        "stop_reason": {
          "type": "string",
          "enum": ["completed", "error", "timeout", "user_stop"],
          "description": "Why the subagent ended. completed: returned a result. error: unrecoverable failure. timeout: idle/total budget exceeded. user_stop: explicit termination."
        },
        "working_directory": {
          "type": "string",
          "description": "Absolute path the parent session was operating in when the subagent finished."
        }
      },
      "required": ["session_id", "stop_reason"],
      "additionalProperties": false
    }
  },
  "required": ["data"]
}
