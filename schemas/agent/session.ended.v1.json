{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://33god.dev/schemas/agent/session.ended.v1.json",
  "title": "Agent Session Ended Event",
  "description": "Emitted when an agent session terminates. Carries summary statistics of the session (turn count, tool histogram, files touched, commits created) so consumers can build retrospective views without replaying every agent.tool.invoked event.",
  "type": "object",
  "allOf": [
    { "$ref": "../_common/cloudevent_base.v1.json" }
  ],
  "properties": {
    "type": {
      "const": "agent.session.ended",
      "description": "Locked event type for this schema."
    },
    "domain": {
      "const": "agent",
      "description": "Locked domain for this schema."
    },
    "data": {
      "type": "object",
      "description": "Session-end payload with aggregate session statistics.",
      "properties": {
        "session_id": {
          "$ref": "../_common/types.v1.json#/$defs/uuid",
          "description": "Identifier of the session that just ended. Matches the session_id from the corresponding agent.session.started event."
        },
        "end_reason": {
          "type": "string",
          "enum": ["user_stop", "completed", "error", "timeout", "context_full"],
          "description": "Why the session ended. user_stop: explicit termination. completed: agent finished its work. error: unrecoverable failure. timeout: idle/total budget exceeded. context_full: hit token limit."
        },
        "duration_seconds": {
          "type": "integer",
          "minimum": 0,
          "description": "Wall-clock seconds between session start and end."
        },
        "total_turns": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of agent turns (tool invocations + responses) during the session."
        },
        "tools_used": {
          "type": "object",
          "description": "Histogram mapping tool name to invocation count for the session.",
          "additionalProperties": {
            "type": "integer",
            "minimum": 0
          }
        },
        "files_modified": {
          "type": "array",
          "description": "Repository-relative paths that were modified (uncommitted) at session end. Best-effort; reflects `git diff --name-only` at the moment the session ended.",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "git_commits": {
          "type": "array",
          "description": "Commit SHAs created during the session, ordered most-recent first. Best-effort; reflects `git log --since=<started_at>` at the moment the session ended.",
          "items": {
            "type": "string",
            "minLength": 7,
            "maxLength": 64
          }
        },
        "final_status": {
          "type": "string",
          "enum": ["success", "failure", "partial"],
          "description": "Outcome classifier. success: goals achieved. failure: blocked or aborted. partial: some goals met."
        },
        "working_directory": {
          "type": "string",
          "minLength": 1,
          "description": "Absolute path the session ran in. Mirrors the value from agent.session.started."
        },
        "git_branch": {
          "type": "string",
          "description": "Git branch at session end. May differ from session.started if the agent switched branches mid-session."
        }
      },
      "required": ["session_id", "end_reason", "duration_seconds", "total_turns"],
      "additionalProperties": false
    }
  },
  "required": ["data"]
}
