{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://33god.dev/schemas/system/heartbeat.tick.v1.json",
  "title": "System Heartbeat Tick",
  "description": "Periodic heartbeat event emitted by a tick service. Consumers use it for liveness monitoring, scheduled task fan-out, restart detection, and as a synthetic load source for testing the v3 event platform. The first real-world domain event in the v3 ecosystem; pattern reference for future domain events.",
  "type": "object",
  "allOf": [
    { "$ref": "../_common/cloudevent_base.v1.json" }
  ],
  "properties": {
    "type": {
      "const": "system.heartbeat.tick",
      "description": "Locked event type for this schema."
    },
    "domain": {
      "const": "system",
      "description": "Locked domain for this schema."
    },
    "data": {
      "type": "object",
      "description": "Heartbeat-specific payload.",
      "properties": {
        "tick_seq": {
          "type": "integer",
          "minimum": 0,
          "description": "Monotonically increasing sequence number from the producer instance. Resets to 0 on producer restart; consumers detect restarts by combining with `started_at`."
        },
        "interval_ms": {
          "type": "integer",
          "minimum": 100,
          "description": "Configured interval between ticks at the producer, in milliseconds. Advisory only; consumers must not assume the next tick will arrive within this window."
        },
        "producer_id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200,
          "description": "Stable identifier for this producer instance. Convention: '<service-name>:<replica-id>' or a UUID generated at startup."
        },
        "started_at": {
          "$ref": "../_common/types.v1.json#/$defs/timestamp",
          "description": "Time the producer instance started. Combined with `producer_id`, lets consumers detect restarts and order overlapping tick streams."
        }
      },
      "required": ["tick_seq", "producer_id", "started_at"],
      "additionalProperties": false
    }
  },
  "required": ["data"]
}
